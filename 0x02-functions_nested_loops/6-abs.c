#include "main.h"
/**
 * _abs - function to find the absolute value of a number
 * @a: function paramter
 * Return: -a or a;
 */

int _abs(int)
{
	if (a < 0)
		return (-a);
	else if (a >= 0)
	{
		return (a);
	}
}
